# saikaku-architecture

## 相性診断の共有互換性

新しく作成する相性診断では、メンバーの別名を `M1`、`M2` …に統一し、画面上だけで本人の表示名へ置き換えます。以前の `A` / `B` 形式で保存された共有結果も引き続き閲覧でき、根拠欄のメンバー見出しには保存済み表示名と別名を併記します。本文中の `A` / `B` は互換性を優先して別名のままとし、表示名へは置き換えません。
